{
  "name": "mycli",
  "version": "0.1.0",
  "private": true,
  "description": "Local browser-bridge CLI with micro-daemon + WebSocket transport. Tampermonkey on the page side.",
  "bin": {
    "mycli": "./bin/mycli.js"
  },
  "scripts": {
    "check": "find src bin clis test -name '*.js' -print0 | xargs -0 -n1 node --check",
    "test": "node --test"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "ws": "^8.18.0"
  },
  "license": "UNLICENSED"
}
